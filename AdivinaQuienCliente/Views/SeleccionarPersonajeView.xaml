<UserControl x:Class="AdivinaQuienCliente.Views.SeleccionarPersonajeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AdivinaQuienCliente.Views"
             mc:Ignorable="d" 
             xmlns:viewmodels="clr-namespace:AdivinaQuienCliente.ViewModels" d:DataContext="{d:DesignInstance Type=viewmodels:MainViewModelCliente}"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="#F4F6F7">
      

        <Border Grid.Column="0" Margin="10" Background="White" CornerRadius="10">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Text="SELECCIONA TU PERSONAJE" 
                   FontSize="18" FontWeight="Bold" Foreground="#2C3E50" 
                   Margin="20,15" HorizontalAlignment="Center"/>

                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                    <ItemsControl ItemsSource="{Binding ListaPersonajes}" Margin="10">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <UniformGrid Columns="6" Rows="4"/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>

                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Button Margin="5" Padding="5" Background="Transparent" BorderThickness="0" IsEnabled="{Binding DataContext.ConPersonaje,RelativeSource={RelativeSource AncestorType=Window} }"  Command="{Binding DataContext.SeleccionarPersonajeCommand,RelativeSource={RelativeSource AncestorType=Window}}" CommandParameter="{Binding Nombre}">
                                    <Border Background="#FDFEFE" BorderBrush="#D5DBDB" BorderThickness="1" CornerRadius="5">
                                        <StackPanel>
                                            <Image Source="{Binding ImagenUrl}" Height="80" Width="80" Stretch="UniformToFill" Margin="0,0,0,5"/>
                                            <TextBlock Text="{Binding Nombre}" FontSize="11" FontWeight="SemiBold" 
                                               HorizontalAlignment="Center" Foreground="#2C3E50" Margin="0,0,0,5"/>


                                        </StackPanel>
                                    </Border>
                                </Button>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </Grid>
        </Border>

     

           


    </Grid>
</UserControl>

<UserControl x:Class="AdivinaQuienServidor.Views.InicioView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AdivinaQuienServidor.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="30">
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Width="400">

            <TextBlock Text="Adivina Quien" 
                   FontSize="24" 
                   FontWeight="Bold" 
                   Foreground="#2C3E50" 
                   HorizontalAlignment="Center" 
                   Margin="0,0,0,5"/>




            <Border Background="#F4F6F7" CornerRadius="10" Padding="15" MinHeight="60">

                <Grid ColumnDefinitions="auto,*">
                    <TextBlock Text="Nombre:" Foreground="Black"  Grid.Column="0" HorizontalAlignment="Center" Padding="10"/>
                    <TextBox Grid.Column="1" Text="{Binding NombreServidor}" Foreground="#2C3E50" />

                </Grid>
            </Border>

            <Button Content="Abrir Sala" 
                Command="{Binding IniciarPartidaCommand}"
                Height="50"
                Background="#27AE60" 
                Foreground="White" 
                FontSize="15" 
                FontWeight="Bold" 
                Margin="0,25,0,0"
                Cursor="Hand">
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="8"/>
                    </Style>
                </Button.Resources>
            </Button>

            <TextBlock Text="* Se requieren 2 jugadores para iniciar" 
                   HorizontalAlignment="Center" 
                   Margin="0,10,0,0" 
                   FontSize="10" 
                   Foreground="#95A5A6" 
                   FontStyle="Italic"/>
        </StackPanel>
    </Grid>
</UserControl>

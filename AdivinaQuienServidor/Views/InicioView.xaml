<UserControl x:Class="AdivinaQuienServidor.Views.InicioView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AdivinaQuienServidor.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="30">
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Width="400">

            <TextBlock Text="SALA DE ESPERA" 
                   FontSize="24" 
                   FontWeight="Bold" 
                   Foreground="#2C3E50" 
                   HorizontalAlignment="Center" 
                   Margin="0,0,0,5"/>

          
            
            <TextBlock Text="JUGADORES EN ESPERA (MAX 2)" FontSize="11" FontWeight="Bold" Foreground="#34495E" Margin="5,0,0,8"/>

            <Border Background="#F4F6F7" CornerRadius="10" Padding="15" MinHeight="150">
                <ItemsControl ItemsSource="{Binding ListaClientes}">
                    <Border Background="White" CornerRadius="5" Padding="10" Margin="0,0,0,5">
                        <Grid>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="💻" Margin="0,0,10,0"/>
                                <TextBlock Text="Cliente_1 (192.168.1.5)" FontSize="12" Foreground="#2C3E50"/>
                            </StackPanel>
                            <TextBlock Text="CONECTADO" HorizontalAlignment="Right" Foreground="#27AE60" FontSize="9" FontWeight="Bold"/>
                        </Grid>
                    </Border>
                </ItemsControl>
            </Border>

            <Button Content="INICIAR PARTIDA" 
                Command="{Binding IniciarPartidaCommand}"
                Height="50"
                Background="#27AE60" 
                Foreground="White" 
                FontSize="15" 
                FontWeight="Bold" 
                Margin="0,25,0,0"
                Cursor="Hand">
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="8"/>
                    </Style>
                </Button.Resources>
            </Button>

            <TextBlock Text="* Se requieren 2 jugadores para iniciar" 
                   HorizontalAlignment="Center" 
                   Margin="0,10,0,0" 
                   FontSize="10" 
                   Foreground="#95A5A6" 
                   FontStyle="Italic"/>
        </StackPanel>
    </Grid>
</UserControl>
